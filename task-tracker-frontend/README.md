# Task Tracker Frontend

React + Vite + Tailwind UI for Task Tracker.

## Quick start

```bash
cd task-tracker-frontend
cp .env.example .env
npm install
npm run dev
```

Backend default: `http://localhost:3000`
